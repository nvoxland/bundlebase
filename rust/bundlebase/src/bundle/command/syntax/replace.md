Replace one attached data source with another. The old file is detached and the new file is attached in its place.

### Examples

    REPLACE 'old_data.csv' WITH 'new_data.csv'
    REPLACE 'sales_2023.parquet' WITH 'sales_2024.parquet'

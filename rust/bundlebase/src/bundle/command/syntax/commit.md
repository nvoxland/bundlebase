Save the current bundle state as a new version with a descriptive message.

### Examples

    COMMIT 'Initial data load'
    COMMIT 'Added user demographics join'
    COMMIT 'Filtered out inactive records'

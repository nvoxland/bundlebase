Remove a persistent connector from the bundle. Optionally drop only a specific platform entrypoint.

### Examples

    DROP CONNECTOR acme.weather
    DROP CONNECTOR acme.weather FOR PLATFORM 'linux/amd64'

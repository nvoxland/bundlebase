Show the contents of a bundle metadata table. A shortcut for
`SELECT * FROM bundle_info.<table>`.

### Examples

    SHOW DETAILS
    SHOW HISTORY
    SHOW STATUS
    SHOW VIEWS
    SHOW INDEXES
    SHOW CONFIG
    SHOW COMMANDS
    SHOW CONNECTORS
    SHOW FUNCTIONS
    SHOW PACKS
    SHOW BLOCKS

Create an index on a column for faster filtering or search. Use COLUMN INDEX for value-based lookups or TEXT INDEX for full-text search.

### Examples

    CREATE COLUMN INDEX ON user_id
    CREATE TEXT INDEX ON description
